#include "bibi.h"

int main()
{

  //debut();
  Smonde *mape = malloc(sizeof(Smonde));

  fuck(mape->plateau, X, Y);
  // act1(mape);

  /* but(mape, 82, 53);
    game(mape);
    printf("AAAAAAAAAAAAALTE LA TEMEEEEEEE T'a cru t'allais monter sur mon pont ou quoi neeeeeeeeeeeeeeeeeeeeeee?!");
 */

  int i = 5;

  if (!yesiam(i))
    printf("ooook\n");

  else
    printf("shiiit\n");

  return 0;
}